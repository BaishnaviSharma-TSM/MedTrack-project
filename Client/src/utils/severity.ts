import type { SeverityLevel } from '@/types';

/** Placeholder severity logic — expanded in Phase 4.2 */
export function computeSeverity(_vitals: Record<string, unknown>): SeverityLevel {
  return 'mild';
}
