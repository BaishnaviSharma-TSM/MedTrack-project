services:
  postgres:
    image: postgres:16-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: medtrack
    volumes:
      - medtrack_pg_data:/var/lib/postgresql/data

volumes:
  medtrack_pg_data:
